# Submission

The four things we hand in. Each folder answers one numbered requirement from the brief. Core submission must be understandable in 15 minutes; appendices go deeper.

| # | Deliverable | Built from |
|---|---|---|
| 1 | `1_decision_doc/` — ICP, channel bet, campaign, distribution loop, activation definition, next experiments | `3_thinking/` |
| 2 | `2_the_build/` — pointer to the artifact, with run instructions | `../pulse/` (public repo, live site) |
| 3 | `3_evidence_appendix/` — sources, discovery notes, assumptions, rejected alternatives | `2_research/` + `3_thinking/hypotheses.md` + `3_thinking/rejected_alternatives.md` |
| 4 | `4_ai_work_log/` — pointer to approach, systems built, where AI was wrong, the decision AI couldn't make | `../5_logs/ai_work_log.md` |

Working session: 20 minutes readout and demo · 20 minutes questions and pushback · 10 minutes changing or extending one part live.

**Status: shipped.** All four deliverables are in place; `2_the_build/` and `4_ai_work_log/` hold pointer READMEs rather than copies, since the CLI is a standalone public repo and the work log is kept live in `5_logs/`.
