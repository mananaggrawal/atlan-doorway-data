# Entries listed here are hidden from the Doorway file tree / agent view.
# Syntax is the same as .gitignore — patterns are matched relative to this file.

# Git internals — never surface these in the UI or to the agent.
.git/

# Tooling / CI configuration that isn't user-editable knowledge.
.github/
.claude/

# Repo hygiene files that clutter the tree without being node content.
.gitignore
.gitattributes
AGENTS.md
# The deployment preamble is edited from External agent access, not as a KB page.
/mcp-description.md
# The pre-rename name. Listed so a knowledge base carrying both files hides
# both — top-up adds AGENTS.md but never deletes the CLAUDE.md beside it.
CLAUDE.md

# Empty-folder placeholders used only for committing empty dirs.
**/.gitkeep

**/access.md
roles.yaml
